# Mobil ilova API manzili (build vaqtida). Sir EMAS — public prefiks.
# Lokal test: kompyuteringizning LAN IP'si (localhost EMAS — qurilma ko'rmaydi).
#   Windows'da IP: ipconfig  ->  IPv4 Address
# Android emulyator uchun maxsus alias: 10.0.2.2 (host localhost'iga ishora).
# iOS simulyator localhost ishlatishi mumkin.
EXPO_PUBLIC_API_URL=http://192.168.1.100:4000
